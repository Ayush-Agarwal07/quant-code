# workspace/reports/

**Status:** scaffold — empty (generated output).

## Holds

Human-readable **Markdown** summaries per run (`run_N.md`) — the judge/devpost
narrative of a run. A presentation projection of the `QuantResearchPacket`, not a
new source of truth.

## Format

Markdown via `WorkspaceManager.write_markdown_report`. Should surface the demo
highlights: feasibility/validation gates, the critique, and (for Run 2) which Tier 3
lesson was retrieved and how it changed the run. Keep the honesty note — experiments
are `not_executed`.

## ❓ Open questions (ask human)

- [ ] Report template/sections — fixed layout or per-command?
- [ ] Auto-generated only, or a hand-editable devpost draft?

## 🧑‍⚖️ HITL checkpoints

- [ ] Before a report makes any performance claim: it must not imply backtested
      results exist. Confirm wording on anything outcome-sounding.
