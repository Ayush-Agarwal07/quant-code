# ============================================================
# QuantCode Dashboard — Frontend Environment Variables
# Copy this file to .env.local to override the API URL.
# ============================================================

# URL of the running read-only QuantCode dashboard API (quantcode dashboard).
# The frontend proxies /api/backend/* to this origin (see next.config.js).
NEXT_PUBLIC_API_URL=http://localhost:8000
